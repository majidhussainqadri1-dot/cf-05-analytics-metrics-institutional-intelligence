# Pull Request Handoff

Open this branch as a Draft Pull Request against `main`. Preserve draft state until exact-head CI, deterministic packaging, source/package parity and review evidence are green. Do not merge or deploy as part of the source-completion batch.
