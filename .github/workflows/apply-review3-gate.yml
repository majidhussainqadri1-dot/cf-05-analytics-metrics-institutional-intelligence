name: Apply CF-05 Review 3 Gate
on:
  push:
    branches: [codex/cf-05-foundation-0.1.0]
    paths: ['.codex/review3-gate.ready']
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          coverage: none
      - name: Apply review 3 gate patch
        run: python3 scripts/apply-review3-gate.py
      - name: Remove one-shot files
        run: rm -f .codex/review3-gate.ready scripts/apply-review3-gate.py .github/workflows/apply-review3-gate.yml
      - name: Full governed QA
        run: bash scripts/qa.sh
      - name: Deterministic package verification
        run: bash scripts/verify-deterministic-build.sh
      - name: Commit review 3 gate patch
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "review 3: bind features to governed Future-40 activation"
          git push origin HEAD:codex/cf-05-foundation-0.1.0
