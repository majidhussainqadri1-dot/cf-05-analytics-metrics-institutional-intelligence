name: Apply CF-05 Review 5 Service Actors
on:
  push:
    branches: [codex/cf-05-foundation-0.1.0]
    paths: ['.codex/review5-service-actors.ready']
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          coverage: none
      - name: Apply review 5 service actor fixes
        run: python3 scripts/apply-review5-service-actors.py
      - name: Remove one-shot files
        run: rm -f .codex/review5-service-actors.ready scripts/apply-review5-service-actors.py .github/workflows/apply-review5-service-actors.yml
      - name: Full governed QA
        run: bash scripts/qa.sh
      - name: Deterministic package verification
        run: bash scripts/verify-deterministic-build.sh
      - name: Commit review 5 fixes
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "review 5: harden Future API governance and actor boundaries"
          git push origin HEAD:codex/cf-05-foundation-0.1.0
